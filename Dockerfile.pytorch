FROM tadejsv/ml-docker:eda

# Install pytorch
RUN  conda install pytorch torchvision cudatoolkit=10.2 -y -c pytorch-nightly && \
     conda install pytorch-lightning && \
     # Some cleanup
     conda clean -yaf && rm -rf /home/.cache